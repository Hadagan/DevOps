pipeline {
    agent {
    label 'aws-jenkins-slave-1'
    }
    stages { 
        stage('Example') {
            steps {
              sh '''#!/bin/bash
              read PARAM_NAME
              case $1 in $PARAM_NAME)
              
              echo "ты выбрал git";;
              $2)
              echo "ты выбрал env";;
              esac
         '''
            }
        }
    }
}
