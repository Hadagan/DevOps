pipeline {
    agent {
    label 'aws-jenkins-slave-1'
    }
      stages { 
        stage('Example') {
            steps {
              sh '''#!/bin/bash
              echo "Вы выбрали $PARAM_NAME"
              echo "----------------------------------------------------"
              case "$PARAM_NAME" in 
              "--git")
              git
              ;;
              "--env")
              env
              ;;
              "--git --env")
              git & echo "----------------------------------------------------" $ env
              ;;
             
            esac
         '''
         
         }
     }
  }   
}
