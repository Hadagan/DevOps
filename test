pipeline {
 agent {
   label 'aws-jenkins-slave-1'
   
 }
   stages {
 
      stage("Check") {
        steps {
          sh 'df -h'
          sh 'cd /home/admin/ && ./berezka 12 31'
          sh "echo $JAVA_HOME"
          sh "java -version"
          sh "git --version"
        }
      }
   }      
}

