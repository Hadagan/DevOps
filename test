pipeline {
 agent {
   label 'aws-jenkins-slave-1'
   
 }
   stages {
 
      stage("Run") {
       bash '''#!/bin/bash
        steps {
        while [ $# -ne 0 ] ; do
        echo "$1"
        shift
        done
        '''
        }
      }
   }      


