pipeline {
    agent {
    label 'aws-jenkins-slave-1'
    }
    stages { 
        stage('Example') {
            steps {
              sh '''#!/bin/bash
              case $1 in $PARAM_NAME)
              shift
              echo "ты выбрал git";;
              $2)
              shift
              echo "ты выбрал env";;
              esac
         '''
            }
        }
    }
}
