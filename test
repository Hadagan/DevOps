pipeline {
 agent {
   label 'aws-jenkins-slave-1'
   
 }
   stages {
 
      stage("Run") {
      steps {
      
       bash '''#!/bin/bash
        while [ $# == ${PARAM_NAME} ] ; do
        echo "$1"
        shift
        done
        '''
        
        }
      }
   }      
}

