pipeline {
 agent {
   label 'aws-jenkins-slave-1'
   
 }
   stages {
 
      stage("Run") {
      steps {
       bash '''#!/bin/bash
        while [ $# -ne 0 ] ; do
        echo "$1"
        shift
        done
        '''
        }
      }
   }      
}

