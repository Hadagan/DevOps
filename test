pipeline {
 agent {
   label 'aws-jenkins-slave-1'
   
 }
   stages {
 
      stage("Run") {
      steps {
      
       bash '''#!/bin/bash
        echo "Hello"
        '''
        
        }
      }
   }      
}

