pipeline {
    agent {
    label 'aws-jenkins-slave-1'
    }
      stages { 
        stage('Example') {
            steps {
              sh '''#!/bin/bash
              echo "ты выбрал $PARAM_NAME"
              case "$1" in 
              
              "--git")
              ecnho "ВЫ ВЫБРАЛИ GIT"
              ;;
            esac
         '''
         
         }
     }
  }   
}
