pipeline {
    agent {
    label 'aws-jenkins-slave-1'
    }
      stages { 
        stage('Example') {
            steps {
              sh '''#!/bin/bash
              echo "ты выбрал $PARAM_NAME"
            case $1 in 
            $PARAM_NAME)
            pwd;;
             
             esac
         '''
         }
     }
  }   
}
