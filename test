pipeline {
    agent {
    label 'aws-jenkins-slave-1'
    }
    stages { 
        stage('Example') {
            steps {
              sh '''#!/bin/bash
              read PARAM_NAME
              case $1 in $PARAM_NAME)
              shift
              echo "ты выбрал git";;
              --env)
              shift
              echo "ты выбрал env";;
              esac
         '''
            }
        }
    }
}
