pipeline {
    agent {
    label 'aws-jenkins-slave-1'
    }
    stages { 
        stage('Example') {
            steps {
              sh '''#!/bin/bash
              read PARAM_NAME
              case $2 in $PARAM_NAME)
              echo "ты выбрал git";;
              $PARAM_NAME)
              echo "ты выбрал env";;
              esac
         '''
            }
        }
    }
}
