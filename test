pipeline {
    agent {
    label 'aws-jenkins-slave-1'
    }
    stages { 
        stage('Example') {
            steps {
                bash '''#!/bin/bash
                 echo "hello world" 
         '''
            }
        }
    }
}
