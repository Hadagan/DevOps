pipeline {
    agent {
    label 'aws-jenkins-slave-1'
    }
    stages { 
        stage('Example') {
            steps {
              sh '''#!/bin/bash
              read PARAM_NAME
              case "$PARAM_NAME" in --git)
              echo "ты выбрал git";;
              --env)
              echo "ты выбрал env";;
              esac
         '''
            }
        }
    }
}
