pipeline {
 agent {
   label 'aws-jenkins-slave-1'
 }
   
  stage('Setting the variables values') {
    steps {
         bash '''#!/bin/bash
                 echo "hello world" 
         '''
    }
    }
}

